@model IEnumerable<ASPNetCoreAPI.BoatType>

@{
    ViewData["Title"] = "Index";
}

<h1>Index</h1>

<p>
    <a asp-action="Create">Create New</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.BoatType1)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.BoatType1)
            </td>
            <td>
                <a asp-action="Edit" asp-route-id="@item.BoatTypeId">Edit</a> |
                <a asp-action="Details" asp-route-id="@item.BoatTypeId">Details</a> |
                <a asp-action="Delete" asp-route-id="@item.BoatTypeId">Delete</a>
            </td>
        </tr>
}
    </tbody>
</table>
